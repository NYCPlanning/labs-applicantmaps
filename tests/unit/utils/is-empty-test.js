import isEmpty from 'labs-applicant-maps/utils/is-empty';
import { module, test } from 'qunit';

module('Unit | Utility | is-empty', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = isEmpty();
    assert.ok(result);
  });
});
