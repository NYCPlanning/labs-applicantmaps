{{yield (hash
  draw-control=(component 'draw-control'
    model=model
    mapInstance=mapInstance
    geometryMode=geometryMode
    selectedLots=selectedLots
    lotSelectionMode=lotSelectionMode)
)}}
