{{#component (concat 'project-geometries/modes/' mode)
  map=map
  geometricProperty=model.proposedGeometry as |edit-mode|
}}
  {{project-geometry-renderer map=map model=model}}

  {{edit-mode.feature-label-form}}

  {{#ember-wormhole to='geometry-type-draw-explainer'}}
    <div class="fade-in-on-load text-small">

      {{back-to-dashboard-button model=model}}

      <h1 class="header-large small-margin-bottom">Rezoning: <span class="text-weight-normal"><br />Special Purpose Districts</span></h1>
      <p><a href="https://www1.nyc.gov/site/planning/zoning/districts-tools/special-purpose-districts.page" target="_blank">Special Purpose Districts</a> supplement and modify the underlying zoning in distinctive neighborhoods with particular issues and goals.</p>

      <hr />

      <p>Special Purpose Districts usually extend to the <strong>street centerline</strong>.</p>
      <div>
        <img src="/img/gif-special-purpose.gif" alt="Draw Special Purpose Districts" class="shadowed" />
      </div>

      <hr />

      {{#edit-mode.button
        enabled=isReadyToProceed
        handleClick=(action 'save')}}
        Save
        <small style="display:block;line-height:1.75;">Special Purpose Districts</small>
      {{/edit-mode.button}}
    </div>
  {{/ember-wormhole}}
{{/component}}

{{#map.source
  options=(mapbox-geojson-source model.proposedGeometry) as |source|}}
  {{source.layer
    layer=proposedSpecialPurposeDistrictsLayer
    before='boundary_country'
  }}
  {{source.layer
    layer=proposedSpecialPurposeDistrictsLabelsLayer
    before='boundary_country'
  }}
{{/map.source}}

{{#map.source
  options=(mapbox-geojson-source model.project.developmentSite) as |source|}}
  {{source.layer
    layer=developmentSiteLayer
    before='boundary_country'
  }}
{{/map.source}}

