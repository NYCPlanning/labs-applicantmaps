{{#if (not-eq model.project.currentStep.step 'complete')}}
  {{#link-to model.project.previousStep.routing.route class="button gray"}}
    {{fa-icon 'arrow-left'}} Back
  {{/link-to}}
{{else}}
  {{#link-to "projects.show" model.project.id class="button gray"}}
    Cancel
  {{/link-to}}
{{/if}}
