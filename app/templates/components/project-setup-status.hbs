<h1 class="header-large">
  {{ember-inline-edit
    value=(readonly project.projectName)
    onSave=(action 'updateProjectName')
    saveButtonClass='button tiny'
    cancelButtonClass='button tiny'
    showEditButton=true
    editButtonClass='button tiny no-margin clear'
  }}
</h1>

<h6>Applicant:</h6>
<p class="header-small">
  {{ember-inline-edit
    value=(readonly project.applicantName)
    onSave=(action 'updateApplicantName')
    saveButtonClass='button tiny'
    cancelButtonClass='button tiny'
    showEditButton=true
    editButtonClass='button tiny no-margin clear'
  }}
</p>

<h6>Project Number:</h6>
<p class="header-small">
  {{ember-inline-edit
    value=(readonly project.zapProjectId)
    onSave=(action 'updateZapProjectId')
    field='number'
    saveButtonClass='button tiny'
    cancelButtonClass='button tiny'
    showEditButton=true
    editButtonClass='button tiny no-margin clear'
  }}
</p>

<hr />

<h6>Steps to be completed:</h6>
<ul class="no-bullet light-gray">
  <li class="
    {{if (gte project.currentStepNumber 1) 'black'}}
    {{if (eq project.currentStepNumber 1) 'text-weight-bold'}}
  ">
    {{fa-icon (if (eq project.currentStepNumber 1) 'arrow-alt-circle-right' 'check')
      fixedWidth=true
      class=(if (gt project.currentStepNumber 1) 'green-muted' '')
    }}
    Development Site
  </li>
  <li class="
    {{if (gte project.currentStepNumber 2) 'black'}}
    {{if (eq project.currentStepNumber 2) 'text-weight-bold'}}
  ">
    {{fa-icon (if (eq project.currentStepNumber 2) 'arrow-alt-circle-right' (if (gt project.currentStepNumber 2) 'check' 'circle'))
      fixedWidth=true
      class=(if (gt project.currentStepNumber 2) 'green-muted' '')
      prefix=(unless (gte project.currentStepNumber 2) 'far' 'fas')
    }}
    Project Area
  </li>
  <li class="
    {{if (gte project.currentStepNumber 3) 'black'}}
    {{if (eq project.currentStepNumber 3) 'text-weight-bold'}}
  ">
    {{fa-icon (if (eq project.currentStepNumber 3) 'arrow-alt-circle-right' (if (gt project.currentStepNumber 3) 'check' 'circle'))
      fixedWidth=true
      class=(if (gt project.currentStepNumber 3) 'green-muted' '')
      prefix=(unless (gte project.currentStepNumber 3) 'far' 'fas')
    }}
    Rezoning
  </li>
</ul>

<hr class="hide-for-large no-margin" />

{{yield}}
