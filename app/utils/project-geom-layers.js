const developmentSiteLayer = {
  type: 'line',
  paint: {
    'line-color': 'green',
    'line-width': 4,
  },
};

const projectAreaLayer = {
  type: 'line',
  paint: {
    'line-color': 'orange',
    'line-width': 4,
  },
};

const rezoningAreaLayer = {
  type: 'line',
  paint: {
    'line-color': 'purple',
    'line-width': 4,
  },
};

export default {
  developmentSiteLayer,
  projectAreaLayer,
  rezoningAreaLayer,
};
